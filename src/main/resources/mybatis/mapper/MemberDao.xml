<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.recruit.jdbc.member.IMemberDao">

	<select id="login"
		resultType="com.example.recruit.jdbc.member.MemberDto">
		SELECT * FROM member
		WHERE mid = #{mid} AND mpw = #{mpw}
	</select>

	<insert id="regist">
		INSERT INTO member (mid, mpw, mname, mbirth, memail, mphone)
		VALUES (#{mid}, #{mpw}, #{mname}, #{mbirth}, #{memail}, #{mphone})
	</insert>

	<select id="idCheck" resultType="int">
		SELECT COUNT(*) FROM member WHERE mid = #{mid}
	</select>

</mapper>