<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.recruit.jdbc.company.ICompanyDao">

	<select id="login"
		resultType="com.example.recruit.jdbc.company.CompanyDto">
		SELECT * FROM company
		WHERE cid = #{cid} AND cpw = #{cpw}
	</select>

	<insert id="regist">
		INSERT INTO company (cid, cpw, cname, csize)
		VALUES (#{cid}, #{cpw}, #{cname}, #{csize})
	</insert>

	<select id="idCheck" resultType="int">
		SELECT COUNT(*) FROM company WHERE cid = #{cid}
	</select>

</mapper>