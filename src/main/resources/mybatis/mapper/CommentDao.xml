<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.recruit.jdbc.job.ICommentDao">


<!-- 댓글 작성 -->
<insert id="insertComment">
	insert into comment1 (comno, jno, mid, content, created_at) 
	values (seq_com.nextVal, #{jno}, #{mid}, #{content}, sysdate)
</insert>

<!-- 댓글 수정 -->
<update id="updateComment">
	update comment1 set content = #{content} where cmono = #{cmono}
</update>

<!-- 댓글 삭제 -->
<delete id="deleteComment">
	delete from comment1 where comno = #{comno}
</delete>

<!-- 댓글 리스트 불러오기 -->
<select id="getComList" resultType="com.example.recruit.jdbc.job.CommentList">
SELECT comno, MID, CONTENT, CREATED_AT FROM COMMENT1 
WHERE JNO = #{jno}
ORDER BY created_at ASC
</select>

</mapper>